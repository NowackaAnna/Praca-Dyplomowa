<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.edu.uwr.runningapp">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PodgladTrening"></activity>
        <activity android:name=".Dziennik" />
        <activity android:name=".TreningElementySzybkosci" />
        <activity android:name=".TreningSilaBiegowa" />
        <activity android:name=".TreningSilowy" />
        <activity android:name=".TreningSprawnosciowy" />
        <activity android:name=".TreningUzupelniajacy" />
        <activity android:name=".TreningRejestrowany" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>